# 版本更新规范

> 本规范对本项目**每一次改动强制生效**。任何一次提交（commit）都必须满足本规范；不满足的改动不得合入 `master`、不得推送到 GitHub。
>
> **每次更新完成后，必须把新版本推送到 GitHub 仓库：<https://github.com/IUNLX/kaoyan-multi-brush>（远端 `origin`，分支 `master`）。更新只停留在本地 = 未完成。**

- 适用项目：考研真题多刷记录（`kaoyan-multi-brush`）
- 远端仓库：<https://github.com/IUNLX/kaoyan-multi-brush>（`origin` / `master`）
- 生效日期：2026-09-25
- 校验命令：`node check-version.mjs`
- 一键发布：`.\release.ps1 "<提交信息>"`

---

## 一、核心原则

1. **单一数据源**：版本号在代码中只有一处真实定义——`考研真题多刷记录.html` 脚本顶部的 `APP_VERSION`。页面标题、首页标签、版本卡片、备份文件名一律由它渲染，**禁止**在别处再写死版本号。
2. **版本号与文档同步**：改了 `APP_VERSION`，就必须同步 `CHANGELOG.md` 与 `README.md`，三者不允许出现任何差异。
3. **可校验**：每次更新结束前必须执行 `node check-version.mjs`，输出「全部一致」才算完成。
4. **数据安全优先**：涉及 `localStorage` 结构变化的改动，必须自带迁移逻辑，且不得让老用户数据丢失。
5. **必须推送**：每次更新完成后，新版本必须推送到 GitHub 仓库 <https://github.com/IUNLX/kaoyan-multi-brush>。**只提交不推送 = 本次更新未完成**。推送后须确认本地与 `origin/master` 齐平。

---

## 二、版本号规则

采用[语义化版本](https://semver.org/lang/zh-CN/) `主版本.次版本.修订号`，**必须写满三段**（写 `1.1` 是错的，应写 `1.1.0`）。

| 位 | 何时 +1 | 本项目示例 |
| --- | --- | --- |
| **主版本** `x.0.0` | 数据结构或使用方式**不兼容**的重大改动 | 成绩记录存储格式重构且旧数据无法自动迁移；拆分多文件、需要构建步骤 |
| **次版本** `x.y.0` | **新增功能**（向下兼容） | 新增科目；新增「总时间可填写」；新增版本信息面板 |
| **修订号** `x.y.z` | **问题修复**、样式微调、文案修正（向下兼容） | 修复导入乱码；修复曲线不刷新；调整配色 |

补充判定：

- 同时包含新功能与修复 → 取**次版本**。
- 纯文档改动（README / CHANGELOG / 代码审查报告）→ **不单独发版**，记入 `[未发布]` 的「文档」条目，随下一次功能发版一起走。
- 拿不准时**取大不取小**，并在 CHANGELOG 中说明理由。
- **禁止跳号**（如 `1.0.0` 直接到 `2.0.0` 而无不兼容改动）、**禁止版本号回退**。

---

## 三、唯一的四处同步点

一次发版必须**同时**改到位，缺一不可：

| # | 位置 | 改什么 |
| --- | --- | --- |
| 1 | `考研真题多刷记录.html` 脚本顶部 | `APP_VERSION` 改为新版本号；`VERSION_HISTORY` **数组开头**插入新版本条目（含 `version` / `date` / `changes`） |
| 2 | `CHANGELOG.md` | 把 `[未发布]` 区段整体下移为 `## [x.y.z] - YYYY-MM-DD`，并新建空的 `[未发布]` |
| 3 | `README.md` | 结尾「版本记录」一行的当前版本与日期 |
| 4 | 校验 | 执行 `node check-version.mjs` 并确保通过 |

> 版本号**只允许**出现在以上 1–3 处。页面标题、`<span class="version">`、版本卡片、备份文件名均已改为运行时渲染，**不要**再去硬编码。

---

## 四、标准更新流程

```
① 改代码 → ② 跑语法自检 → ③ 定版本号并同步三处 → ④ 写 CHANGELOG → ⑤ 跑校验 → ⑥ 提交 → ⑦ 推送并验证
```

> 第 ①–⑦ 步可用一条命令完成（仍建议先自己跑一遍 ⑤ 看结果）：
> ```powershell
> .\release.ps1 "feat: 本次改了什么"
> ```

**第 ① 步：改代码**
只改 `考研真题多刷记录.html`（单文件应用，无构建步骤）。

**第 ② 步：语法自检**
改完 HTML 内联脚本后立即检查，避免把语法错误提交上去：

```powershell
node -e "const fs=require('fs'),vm=require('vm');const m=fs.readFileSync('考研真题多刷记录.html','utf8').match(/<script>([\s\S]*?)<\/script>/);new vm.Script(m[1]);console.log('语法检查通过')"
```

**第 ③ 步：定版本号并同步三处**
按第二节规则确定 `x.y.z`，然后改第三节表格中的 1、2、3。

**第 ④ 步：写 CHANGELOG**
按 `CHANGELOG.md` 内已有的「记录规则」与条目模板填写。

**第 ⑤ 步：跑校验**

```powershell
node check-version.mjs
```

必须看到 `✅ 全部一致`。任何 `❌` 都必须先修好再提交。

**第 ⑥ 步：提交**
提交信息格式见第五节。**提交前确认 `考研真题多刷记录.html - 快捷方式.lnk` 未被勾选**（见第九节）。

**第 ⑦ 步：推送到 GitHub（不可省略）**

```powershell
git push origin master
git status -sb   # 必须显示 ## master...origin/master，不带 ahead
```

推送完成后，到 <https://github.com/IUNLX/kaoyan-multi-brush/commits/master> 确认最新提交已出现。

> 判定标准：`git log origin/master..master --oneline` **输出为空**，才算推送完成。详见第六节。

---

## 五、提交信息规范

格式：

```
<类型>: <简短描述>

<可选的详细说明>
```

已有提交沿用了此风格，保持一致：

| 类型 | 用途 |
| --- | --- |
| `feat` | 新增功能（对应次版本） |
| `fix` | 修复问题（对应修订号） |
| `docs` | 仅文档改动 |
| `refactor` | 重构，行为不变 |
| `style` | 样式、格式调整 |
| `chore` | 构建、配置、杂项 |

要求：

1. 描述用**中文**，一句话说清「改了什么」，不超过 50 字。
2. 标题**不加句号**。
3. **涉及发版的提交，在正文中写明版本号**，例如：

```
feat: 试卷总时间改为可填写，未填写显示「其他时间」

v1.1.0
- 「时间(分)」列四科均改为可输入框，留空显示「其他时间」
- 新增 timeManual 标记，统一 totalTime()，修正显示值与导出值不一致
- 旧数据载入时自动用分项合计补齐总时间
```

4. 一个提交只做一件事；发版提交应包含该版本的全部改动。

---

## 六、推送到 GitHub（每版必做）

**每次更新完成后必须推送。** 目标是仓库 <https://github.com/IUNLX/kaoyan-multi-brush>，远端名 `origin`，分支 `master`。

### 6.1 标准推送

```powershell
git push origin master
```

### 6.2 推送后必须验证

```powershell
git status -sb                      # 期望：## master...origin/master（无 ahead）
git log origin/master..master --oneline   # 期望：无任何输出
```

两条都满足才算推送成功。也可直接在浏览器打开 <https://github.com/IUNLX/kaoyan-multi-brush/commits/master> 核对最新提交。

### 6.3 一键发布脚本

`release.ps1` 会把「校验 → 暂存 → 提交 → 推送 → 验证」串起来：

```powershell
.\release.ps1 "feat: 本次改了什么"
```

它会依次完成：

1. 执行 `node check-version.mjs`，**不通过就直接中止**，不会提交、不会推送；
2. 列出将要提交的文件并请你确认（加 `-Yes` 跳过确认，适合熟练后使用）；
3. `git commit` + `git push origin master`；
4. 再次校验本地与 `origin/master` 是否齐平，并打印 GitHub 提交页地址。

若 PowerShell 提示脚本被禁止运行，用：

```powershell
powershell -ExecutionPolicy Bypass -File .\release.ps1 "feat: 本次改了什么"
```

### 6.4 推送失败怎么办

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| `Authentication failed` / 要求登录 | GitHub 凭据失效 | 重新登录 Git 凭据管理器，或改用 GitKraken 的 Push 按钮触发一次登录 |
| `SSL certificate ... unable to get local issuer certificate (20)` | 本机加速器（Steam++ / Watt Toolkit）把 github.com 写入 hosts 指向 127.0.0.1 并用自签根证书转发，而 Git 默认的 `openssl` 后端只认自带 CA 包 | 改用系统证书库：`git config http.sslBackend schannel`（**本仓库已设置**）。参见 6.6 |
| `schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS` | 在受限沙箱 / 非交互环境下无法访问系统证书库或凭据 | 在普通终端（或 GitKraken）里执行推送 |
| `couldn't create signal pipe` / `failed to execute prompt script` | 受限环境下凭据助手无法弹出登录窗口 | 同上，改到普通终端推送 |
| `rejected ... non-fast-forward` | 远端有你本地没有的提交（如另一台设备推过） | 先 `git pull --rebase origin master`，解决冲突后重新推送 |
| `Could not resolve host` | 网络 / 代理问题 | 检查网络；能打开 GitHub 后重试 |
| 推送成功但 GitHub 页面没更新 | 看的是缓存页面 | 强制刷新 `Ctrl+F5`，或核对提交哈希 |

### 6.5 禁止

- ❌ 更新做完却**只提交不推送**，让它留在本地。
- ❌ 版本号 / CHANGELOG / README 不一致就推送（第六节 6.3 的脚本已自动拦截）。
- ❌ 用 `git push --force` 覆盖远端历史（除非明确知道后果并已备份）。

### 6.6 本机已知环境问题：加速器与证书

本机装有 **Steam++（Watt Toolkit）**，它会：

1. 把 `github.com` 等域名写入 `C:\Windows\System32\drivers\etc\hosts` 指向 `127.0.0.1`；
2. 在本机 `443` 端口起反向代理转发这些域名的流量；
3. 用自签根证书 `CN=SteamTools Certificate`（已装入 `LocalMachine\Root`）解密流量。

Git for Windows 默认的 `openssl` 后端只信任自带的 CA 包（`<Git安装目录>\mingw64\etc\ssl\certs\ca-bundle.crt`），**不读 Windows 证书库**，因此会报：

```
SSL certificate problem: unable to get local issuer certificate (20)
```

**解决办法**（已对本仓库配置好，无需重复操作）：

```powershell
git config http.sslBackend schannel   # 只改本仓库，不动全局配置
```

`schannel` 后端使用 Windows 证书库，因而信任 Steam++ 的根证书。若换到新机器或新克隆的仓库再遇到此错误，重跑上面这条命令即可。

> 注意：该错误与代码、版本号无关，不要用 `git config --global http.sslVerify false` 绕过——那会关闭所有仓库的证书校验。

### 6.7 自动推送（Git 钩子，已启用）

为免去「提交后忘记推送」，本仓库已启用 Git 钩子：

```powershell
git config core.hooksPath .githooks   # 已配置
```

钩子脚本随仓库一起版本管理（`.githooks/`），不是放在不入库的 `.git/hooks/` 里：

| 钩子 | 作用 | 失败后果 |
| --- | --- | --- |
| `.githooks/pre-commit` | 当 `考研真题多刷记录.html` / `CHANGELOG.md` / `README.md` 被暂存时，自动执行 `node check-version.mjs` | **拦截提交**（版本号不一致不得入库） |
| `.githooks/post-commit` | 提交后自动 `git push origin <当前分支>` | 仅打印提示，**不影响提交**（提交仍保留在本地） |

**跳过方式**（确有必要时）：

```powershell
git commit --no-verify                        # 跳过本次钩子
$env:KAOYAN_SKIP_VERSION_CHECK=1              # 只跳过版本校验
$env:KAOYAN_NO_AUTOPUSH=1                     # 只跳过自动推送
```

设计取舍：

- 推送失败**不会**让提交失败，避免网络问题导致无法提交；
- rebase / cherry-pick 过程中的中间提交不推送，等整体完成后再推；
- 未配置远端或处于分离头指针状态时自动跳过。

> ⚠️ **受限环境（沙箱 / 无 `sh` 的环境）无法执行 shell 钩子**。Git for Windows 通过 `sh.exe` 运行钩子，若该进程被策略禁止，`git commit` 会因钩子无法启动而报错。此时用 `git commit --no-verify` 提交，再手动 `git push`。在普通终端与 GitKraken 中不受影响。

---

## 七、数据兼容与迁移要求

改到 `localStorage` 相关结构时，**必须**满足：

1. **只增不减**：新增字段可以，删除或重命名已有字段必须配迁移。
2. **载入即迁移**：在 `loadData()` / `loadPlan()` 中补默认值，老数据打开后自动升级，用户无感。
3. **版本标记**：结构变化时同步提升存储内的 `v` 字段（如 `v:2`），并按旧值分支处理。
4. **迁移必须在 CHANGELOG 中写明**（这是 `CHANGELOG.md` 记录规则第 4 条的硬性要求）。
5. **不得静默丢数据**：宁可保留脏字段，也不能让用户已录入的成绩消失。
6. 改动存储结构时，主版本或次版本至少 +1，并在提交正文说明「是否需要用户额外操作」（理想情况是**不需要**）。

---

## 八、更新完成自检清单

每次更新提交前逐条确认：

- [ ] HTML 内联脚本语法检查通过（第四步 ②）
- [ ] `APP_VERSION` 是完整三段式版本号
- [ ] `VERSION_HISTORY` 首条与 `APP_VERSION` 一致，日期为真实发布日
- [ ] `CHANGELOG.md` 已新建对应版本区段，`[未发布]` 已重置
- [ ] `README.md` 的当前版本与日期已同步
- [ ] 涉及数据结构变化时，迁移逻辑已写且已在 CHANGELOG 说明
- [ ] `node check-version.mjs` 输出 `✅ 全部一致`
- [ ] 浏览器 `Ctrl+F5` 实测：改动能用，且老数据仍在
- [ ] 提交信息符合第五节格式
- [ ] `考研真题多刷记录.html - 快捷方式.lnk` 未被勾选（该文件应处于「已取消跟踪」状态）
- [ ] **已推送**：`git log origin/master..master --oneline` 输出为空
- [ ] 已在 <https://github.com/IUNLX/kaoyan-multi-brush/commits/master> 看到本次提交

---

## 九、禁止事项

1. ❌ 在 `APP_VERSION` 之外**任何地方**硬编码版本号。
2. ❌ 只改代码不改 CHANGELOG / README 就提交发版。
3. ❌ 提交 `考研真题多刷记录.html - 快捷方式.lnk`（本机快捷方式，属于个人环境文件）。`.gitignore` 已加入 `*.lnk` 防止再次误加；该文件此前已被误提交，需执行 `git rm --cached "考研真题多刷记录.html - 快捷方式.lnk"` 取消跟踪（本地文件不受影响）。
4. ❌ 提交**新增**的临时 / 中间 Excel 文件（`.gitignore` 已忽略 `*.xlsx`）。
   > 例外：项目对外发布的三个记录表 `考研多刷记录表.xlsx`、`考研总结与计划表.xlsx`、`考研真题多刷记录表.xlsx` 是 README 中列明的既有交付物，且**已在版本管理中**，属正常文件，不要删除。`.gitignore` 只拦截今后新增的 xlsx。
5. ❌ 未经实测（浏览器 `Ctrl+F5` 打开确认）就提交功能性改动。
6. ❌ 版本号跳号或回退。
7. ❌ 为同一批改动重复发版（一次改动对应一个版本号）。

---

## 十、常见场景速查

| 场景 | 版本变化 | 备注 |
| --- | --- | --- |
| 新增一个科目 | `1.1.0 → 1.2.0` | 需在 `CONFIG` 加配置，并确认备份键名 |
| 修复导出 CSV 乱码 | `1.1.0 → 1.1.1` | 纯修复 |
| 调整按钮配色 | `1.1.0 → 1.1.1` | 样式微调归修订号 |
| 成绩记录存储格式重构 | `1.1.0 → 2.0.0` | 必须带迁移逻辑 |
| 只更新 README | 版本号不变 | 记入 `[未发布]` 的文档条目 |
| 新增「导入 Anki 卡片」功能 | `1.1.0 → 1.2.0` | 新功能归次版本 |

---

## 十一、违规处理

- `node check-version.mjs` 报错的改动，**不得提交、不得推送**。
- **更新完成后未推送的，视为本次更新未完成**，必须立即补推送；不允许把未推送的提交留在本地过夜。
- 已推送后发现版本号不一致：**立即补一个 `fix:` 提交**修正并推送，不允许留在仓库里过夜。
- 校验脚本本身发现遗漏的检查项时，**先补脚本再继续开发**，避免规范空转。

---

相关文档：[README.md](./README.md) ｜ [CHANGELOG.md](./CHANGELOG.md) ｜ 远端仓库：<https://github.com/IUNLX/kaoyan-multi-brush>
