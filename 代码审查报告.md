# 代码审查报告

**审查对象**：`考研真题多刷记录.html`（1878 行，单文件应用）+ 配套文档与数据文件
**审查日期**：2026-09-22
**审查方式**：全文逐行阅读 + Node 侧函数级复现测试 + 静态扫描（语法、未声明调用、DOM id、CSS 类）

## 结论概览

| 等级 | 数量 | 含义 |
| --- | --- | --- |
| P0 严重 | 2 | 会造成数据丢失或功能实际不可用 |
| P1 中等 | 5 | 边界行为错误、静默失败、语义不符 |
| P2 轻微 | 6 | 体验缺陷、残留代码、可维护性 |
| P3 工程/文档 | 6 | 版本控制、文档准确性、无障碍 |

代码整体质量对得住「单文件零依赖」的定位：ES5 风格统一、数据归一化完善、无 XSS 面、无未定义调用。**主要风险集中在"静默失败"这一类**——出错时不报错、不拦住，直接把用户数据冲掉或让功能以为在工作。

---

## P0-1 数据损坏时静默清零（最严重）

**位置**：`loadData()` 第 536–545 行、第 561 行

```js
try{
  var raw=localStorage.getItem(storeKey());
  if(raw){var d=JSON.parse(raw);
    if(Array.isArray(d))state=migrateOld(d);
    else if(d&&Array.isArray(d.papers)&&Array.isArray(d.records))state=d;
  }
}catch(e){}                    // ← 解析异常被吞掉
if(!state)state=buildDefault(); // ← 走默认空数据
...
papers=state.papers;records=state.records;
syncRounds();
saveData();                     // ← 立刻把空数据写回，覆盖原始内容
```

**问题**：只要该科目的 localStorage 值损坏（JSON 被截断、手改出错、浏览器写入中断），或结构不符合预期（例如 `papers` 在但 `records` 缺失），就会：

1. 静默走 `buildDefault()`，得到一套空白记录；
2. 紧接着 `saveData()` **把空白数据写回同一个键**，原始内容被永久覆盖；
3. 全程没有任何提示，用户只看到"成绩没了"。

对记录型工具来说这是最高危的一类缺陷——**唯一副本 + 静默覆盖**。

**建议**：区分「没有数据」与「有数据但读不出来」：

```js
var raw=localStorage.getItem(storeKey()), parseError=false;
if(raw){
  try{ ... }catch(e){ parseError=true; }
  if(parseError||!state){
    // 先把原始内容另存一份再继续，绝不直接覆盖
    try{localStorage.setItem(storeKey()+'_corrupt_'+Date.now(), raw);}catch(e){}
    alert('检测到本机数据损坏，已备份原始内容并重建空白表。\n如需抢救数据请联系备份文件恢复。');
  }
}
```

另外 `v!==2` 分支（546–551）用 `filter(roundHasData)` 静默丢弃"空占位"记录，逻辑本身合理，但同样建议在丢弃条数 > 0 时留痕。

---

## P0-2 「总结与计划提醒」在其主场景下完全失效

**位置**：`renderTodayPlan()` 第 1598 行提前返回；`renderReminder()` 定义于 1733 行，**唯一调用点**是 1630 行

```js
function renderTodayPlan(){
  ...
  if(!hits.length){card.style.display='none';list.innerHTML='';return;}  // ← 在此返回
  ...
  renderReminder();   // ← 只有走到这里才会刷新提醒卡
}
```

**问题**：`renderReminder()` 是全项目唯一负责渲染「总结与计划提醒」卡片的函数，而它只在 `renderTodayPlan()` 的末尾被调用。当**今天没有计划覆盖**时（第 1598 行）直接 return，提醒卡永远不被渲染，HTML 里它默认 `display:none`，于是：

- 提醒功能只在"今天有正在执行的计划"时才可能出现；
- 而它要提醒的恰恰是**「周期已结束、该做总结 / 该开新一轮计划」**——此时通常没有覆盖今天的计划。

**即该功能在最需要它的场景下 100% 不工作。** 这不是偶发，是稳定复现。

**建议**：把 `renderReminder()` 从 `renderTodayPlan()` 中拆出，两者各自独立调用：

```js
function renderTodayPlan(){
  ...
  var card=document.getElementById('todayPlanCard');
  if(!hits.length){card.style.display='none';list.innerHTML='';}
  else{ card.style.display='block'; /* 渲染列表 */ }
}
// DOMContentLoaded、savePlanEntry、删除、导入后：
renderTodayPlan(); renderReminder();
```

---

## P1-1 手动选择开始日时，"每周"不再对齐周一到周日

**位置**：`snapEnd()` 第 1311 行 vs `dateRangeFor()` 第 1242 行

`dateRangeFor('week')` 自动生成时严格对齐周一–周日；但用户在弹窗里手改开始日期时，`snapEnd` 一律按 `开始日 + 6 天` 计算结束日。Node 复现结果：

| 手动选择开始日 | snapEnd 生成的结束日 | 实际区间 |
| --- | --- | --- |
| 2026-09-21（周一） | 2026-09-27（周日） | 周一–周日 ✅ |
| 2026-09-23（周三） | 2026-09-29（周二） | **周三–周二 ❌** |
| 2026-09-27（周日） | 2026-10-03（周六） | **周日–周六 ❌** |

自动生成时为 `2026-09-21 ~ 2026-09-27`。**同一维度下两套口径**，且 `README.md` 也写明"周为周一到周日"，属实现与文档同时不符。

**建议**：`snapEnd` 的 week 分支改为先把开始日吸附到当周周一，再 +6；或结束日吸附到当周周日：

```js
if(level==='week'){
  var dow=d.getDay()||7;                 // 周一=1 … 周日=7
  var mon=new Date(y,m,day-dow+1);       // 当周周一
  return dateToStr(new Date(mon.getFullYear(),mon.getMonth(),mon.getDate()+6));
}
```

---

## P1-2 非法日期被静默"顺延"，不报错也不纠正

**位置**：`strToDate()` 第 1225 行

```js
function strToDate(s){var p=String(s||'').split('-');if(p.length<3)return null;return new Date(+p[0],+p[1]-1,+p[2]);}
```

`new Date(y, m, d)` 对越界值自动进位，实测：

| 输入 | 实际解析为 |
| --- | --- |
| `2026-02-30` | **2026-03-02** |
| `2026-13-01` | **2027-01-01** |
| `2026-00-10` | **2025-12-10** |
| `abc` | `null`（正确） |

影响面：`fmtRange()` 显示的时间段、`renderReminder()` 第 1783 行的「已过期 N 天」计算，以及 CSV / JSON 导入进来的任意日期字符串（导入路径不做日期校验）。用户会看到凭空多出/少掉的天数，且无从判断原因。

**建议**：加一次往返校验：

```js
function strToDate(s){
  var p=String(s||'').split('-');if(p.length!==3)return null;
  var y=+p[0],m=+p[1],d=+p[2];
  if(!y||!m||!d)return null;
  var dt=new Date(y,m-1,d);
  if(dt.getFullYear()!==y||dt.getMonth()!==m-1||dt.getDate()!==d)return null; // 拒绝 2026-02-30
  return dt;
}
```

---

## P1-3 在月末当天选「每月」，整月塌缩成单日

**位置**：`snapEnd()` 第 1313 行 `return dateToStr(new Date(y,m+1,0));`

实测：

| 手动开始日 | snapEnd(month) 结束日 | 区间长度 |
| --- | --- | --- |
| 2026-03-15 | 2026-03-31 | 17 天（可接受） |
| **2026-01-31** | **2026-01-31** | **1 天 ❌** |
| **2026-02-28** | **2026-02-28** | **1 天 ❌** |

同理半月维度若开始日选 15 日之后会得到不规则区间（`2026-01-16 → 01-31`，已属设计口径）。月末塌缩会让统计和提醒都失真。

**建议**：月末/月首特殊处理，或把「每月」的开始日吸附到当月 1 日：

```js
if(level==='month'){
  var last=new Date(y,m+1,0).getDate();
  if(day===last) return dateToStr(new Date(y,m,last)); // 仍为单日则提示
  return dateToStr(new Date(y,m+1,0));
}
```

更稳妥的做法是吸附开始日：`每月` → 当月 1 日；`每半月` → 1 日或 16 日。

---

## P1-4 存储写入失败被静默吞掉，界面仍在"假装保存"

**位置**：`saveData()` 563 行、`savePlan()` 1351 行

```js
function saveData(){try{localStorage.setItem(storeKey(),JSON.stringify({...}));}catch(e){}}
```

触发场景真实存在：无痕/隐私模式、浏览器存储配额耗尽（5MB）、站点数据被策略禁用。此时**每一次录入都写入失败，但 UI 照常回显、表格照常更新**，用户可能连续录入数小时后关闭页面，数据全部消失。

另外两处写入甚至没有 try/catch，异常会直接中断事件处理函数：

- 第 1813 行 `localStorage.setItem(targetKey(), e.target.value)`（目标分）
- 第 1816 行 `localStorage.setItem('kaoyan_accent_'+cfg().id, v)`（配色）

**建议**：统一封装写入并在失败时**显著告警一次**（避免每次按键都弹窗）：

```js
var storageWarned=false;
function safeSet(k,v){
  try{localStorage.setItem(k,v);return true;}
  catch(e){
    if(!storageWarned){storageWarned=true;alert('警告：本机存储写入失败，你的改动不会被保存！\n请立即用「一键备份」导出，并检查是否处于无痕模式或存储已满。');}
    return false;
  }
}
```

---

## P1-5 「一键恢复」的"覆盖"语义不完整，且不校验版本

**位置**：`restoreAll()` 第 1131–1141 行；备份载荷版本号在 1125 行；确认文案在 1804 行

```js
Object.keys(d.data).forEach(function(k){
  if(/^kaoyan_/.test(k)&&d.data[k]!=null){localStorage.setItem(k,d.data[k]);n++;}
});
```

两个问题：

1. **`!=null` 跳过空值** → 备份中为 `null` 的键（该科目从未使用）会保留**当前本机**的数据，而确认框却写"恢复将覆盖当前所有数据"。跨设备恢复时会出现"部分键是备份的、部分是旧设备的"混合状态，不易察觉。
2. **完全忽略 `d.v`**（备份格式版本，当前为 `1`）→ 将来格式升级后，旧备份会被无条件写入，无法给出兼容性提示。

**建议**：恢复前先清空所有 `kaoyan_*` 键再写入，并校验版本：

```js
if(d.v>1){alert('该备份由更新版本导出，可能不兼容');return;}
Object.keys(localStorage).filter(k=>/^kaoyan_/.test(k)).forEach(k=>localStorage.removeItem(k));
Object.keys(d.data).forEach(function(k){ if(/^kaoyan_/.test(k)) localStorage.setItem(k,d.data[k]); });
```

---

## P2 级问题

### P2-1 按刷次筛选时无法添加新刷次
`renderTable()` 第 697–700 行把 `＋`（添加刷次）按钮只挂在每套试卷的**第一行**（`roundIdx===0`）。`applyFilter()` 按刷次隐藏行，因此当筛选为"三刷"时，一刷行被隐藏，`＋` 按钮一并消失，用户必须先切回"全部"才能加刷次。建议把 `＋` 同时放到筛选后可见的行，或在筛选状态下禁用筛选。

### P2-2 历史周期的提醒永不消失
`renderReminder()` 第 1744–1747 行：只要某条**计划**已过结束日且找不到同开始日的总结，就永久提示"该进行总结了（已过期 N 天）"。没有"忽略/已读"机制，使用半年以上会累积大量过期提醒（每周/半月/月各一条），把真正紧急的挤下去。建议只提醒最近 1–2 个周期，或增加"忽略"按钮（写入一个 dismissed 集合）。

### P2-3 `.reminder-item` / `.reminder-text` 是死类
静态扫描确认：第 1775、1781 行给元素加了这两个 className，但整份样式表（7–190 行）**没有任何对应规则**。目前靠 `.today-item` 兜底所以外观正常，但说明提醒项原本特有的样式丢失了。建议补样式或删掉这两个类名。

### P2-4 `fmt()` 遇到 `undefined` 会抛 TypeError
第 577 行 `function fmt(v){return v===null?'—':(Number.isInteger(v)?v:v.toFixed(1));}` —— 只判断了 `null`，未覆盖 `undefined`。当前所有内部路径都已归一化为 `null`（`loadData` 552 行做了补齐），所以暂时不会触发；但一旦用户手工编辑 localStorage 或从外部导入造成 `s` 数组偏短，`undefined.toFixed(1)` 会直接抛异常中断渲染。改成 `v==null` 即可零成本加固。

### P2-5 旧格式迁移会丢字段
`migrateOld()` 第 532 行构造记录时写死 `times:null, time:null, date:null`，只保留 `r.year / r.round / r.s / r.note`。若旧数组格式中存在日期或用时字段，迁移后即丢失。建议按可选字段读取：`date:r.date||null, time:r.time==null?null:r.time`。

### P2-6 分项得分步长与题型不匹配
第 706 行所有题型输入框统一 `step=0.5`。政治单选（每题 1 分）、多选（每题 2 分）按 0.5 分录入不符合实际评分规则；反之主观题确有半分需求。属可接受的宽松设计，但未在界面上说明，建议按题型配置 step 或至少加提示。

---

## P3 工程与文档问题

### P3-1 无版本控制 ★
目录内**没有 `.git`**，却在手工维护 `CHANGELOG.md`。单文件应用恰恰最适合 git：每次改动一次提交，`git diff` 就能替代人工比对，也能避免"改崩了回不去"。建议：

```
git init
git add -A
git commit -m "chore: 初始版本 v1.0.0"
```

### P3-2 README 的 ECharts 版本写错了 ★
实测本地 `echarts.min.js` 内部版本号为 **5.4.4**（文件首行 `version:"5.4.4"`），而：

- `README.md` 写的是「ECharts 5.4.3 本地副本」；
- `考研真题多刷记录.html` 第 1868–1870 行三个 CDN 回退全部请求 **5.4.3**。

即**离线用 5.4.4、联网回退用 5.4.3**，同一份数据在不同环境下可能渲染出细微差异，且文档与实物不符。建议统一到同一版本（推荐把 CDN 也改成 5.4.4 或把本地文件换成 5.4.3），并修正 README。

### P3-3 无 LICENSE
见上一轮已提示。若会分享给他人，建议补 MIT 并在 README 增加许可章节。

### P3-4 三个 Excel 文件内容重叠
| 文件 | 工作表 |
| --- | --- |
| `考研真题多刷记录表.xlsx` | 13 个（首页 + 各科记录/数据/曲线） |
| `考研多刷记录表.xlsx` | 5 个（使用说明 + 四科） |
| `考研总结与计划表.xlsx` | 2 个（总结与计划 + 使用说明） |

前两个用途高度重叠，且都被网页版取代。建议只保留信息最全的一份，其余归档或删除，减少"到底该用哪个"的困惑。

### P3-5 无障碍（a11y）缺口
- 首页科目卡片是 `<div class="subject-card">` + click 监听（第 1810 行），**无 `tabindex`、无 `role="button"`、无键盘事件** → 键盘用户完全无法进入任何科目页。
- 三个模态框无 `role="dialog"` / `aria-modal="true"`，不支持 Esc 关闭，无焦点陷阱（Tab 会跑到背景元素上）。
- 成绩表格的输入框无 `<label>` 或 `aria-label`，读屏只报"数字输入框"。

个人工具可以接受，但改动成本很低；至少给卡片加 `tabindex="0" role="button"` + Enter/Space 监听。

### P3-6 移动端未作说明
第 57 行 `table{min-width:1120px}`，手机上必须横向滚动；首页卡片在 ≤900px 折成两列。这属于合理取舍（记录表本来就宽），但 README 只写"推荐 Chrome / Edge 等现代浏览器"，未说明**以桌面使用为主**，也没给最低版本（代码用到 `Element.closest`、`NodeList.forEach`，需 Chrome 51+ / Edge 15+）。

---

## 已确认无问题的部分（正面结论）

这些是我主动验证过、**没有**发现问题的方面，供你放心：

| 检查项 | 方法 | 结果 |
| --- | --- | --- |
| JS 语法 | `new Function()` 解析整个 script | ✅ 通过，无语法错误 |
| 未声明函数调用 | 提取全部定义与调用点做差集 | ✅ 无（`fn`/`cb` 是形参、`translate(` 来自字符串，均为误报） |
| 死函数 | 107 个函数/函数变量 vs 调用点 | ✅ 无「声明但从未调用」 |
| DOM id 引用 | 正则提取 HTML 的 id 与 JS 的 `getElementById` 求差 | ✅ 全部存在（`stat_done` 由 `statCard()` 动态创建） |
| **XSS** | 逐条审查用户数据的写入方式 | ✅ **无风险**：试卷名、备注、计划文本一律走 `textContent` / `createTextNode` / `input.value`；`innerHTML` 仅用于 `CONFIG` 里的静态题型名 |
| 数据归一化 | 审查 `loadData` 552 行 | ✅ 完备：`s`/`times` 长度补齐、越界截断、`note`/`time`/`date` 归 null |
| 恢复键名安全 | 审查 `restoreAll` | ✅ 正则 `/^kaoyan_/` 前缀白名单，不会写入任意键 |
| CSV 往返 | 审查 `exportCSV`/`importCSV` | ✅ 导出带 BOM 防乱码、导入识别 GBK、保留「参与」状态、越界分数自动 clamp |
| 刷次删除保护 | 审查 `removeRound` | ✅ 「一刷」不可删、至少保留一刷、已填内容二次确认 |
| 图表与筛选一致性 | 审查 `drawCharts` | ✅ 曲线、得分率图、统计卡统一以 `activePapers()` 为准，「不参与」试卷被正确排除 |
| 资源泄漏 | 审查 Blob URL 生命周期 | ✅ `revokeObjectURL` 均有 setTimeout 兜底释放 |

---

## 建议修复顺序

| 顺序 | 内容 | 理由 | 预估工作量 |
| --- | --- | --- | --- |
| 1 | P0-1 数据损坏不再静默覆盖 | 唯一副本 + 永久丢失，风险最高 | 小 |
| 2 | P0-2 拆分 `renderReminder` 调用 | 一行位置调整即可救活一个完整功能 | 很小 |
| 3 | P1-4 存储写入失败告警 | 防止"以为在保存"的长时间白录 | 小 |
| 4 | P1-1 / P1-2 / P1-3 日期逻辑三处 | 影响统计与提醒准确性 | 小 |
| 5 | P1-5 恢复语义 + 版本校验 | 跨设备恢复的正确性 | 小 |
| 6 | P3-1 `git init` + P3-2 修正 README 版本号 | 低成本高收益 | 很小 |
| 7 | P2 各项 | 体验优化 | 中 |

> 以上均为**审查发现**，本轮未改动 `考研真题多刷记录.html` 任何代码。需要我按该顺序动手修复的话，说一声即可——建议 1、2、6 先做，改动小且直接消除数据风险。
