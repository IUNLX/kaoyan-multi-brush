# 版本修改记录

本文件记录 **考研真题多刷记录** 项目所有值得注意的改动。

- 格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)
- 版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.修订号`
  - **主版本**：数据结构或使用方式不兼容的重大改动
  - **次版本**：新增功能（向下兼容）
  - **修订号**：问题修复、样式微调等（向下兼容）

## 记录规则

1. 每次改动都在 `[未发布]` 区段下新增条目，写完后再改成正式版本号。
2. 条目按类型归类：`新增` / `变更` / `修复` / `移除` / `安全`。
3. 日期统一使用 `YYYY-MM-DD` 格式。
4. 涉及数据存储格式变化的改动，**必须**在条目中说明升级/迁移方式。
5. 每次发版后把 `[未发布]` 区段内容整体下移为新的版本区段，并补上日期与版本号链接。

## [未发布]

### 新增
- 《版本更新规范.md》新增 6.6 节「本机已知环境问题：加速器与证书」，记录 Steam++（Watt Toolkit）接管 GitHub 域名导致 `unable to get local issuer certificate` 的成因与 `git config http.sslBackend schannel` 解决办法；6.4 节补充对应故障行。

### 变更
- （暂无）

### 修复
- （暂无）

---

## [1.1.0] - 2026-09-25

### 新增
- **试卷总时间可填写**：「时间(分)」列四科均改为输入框，可直接填写本套试卷总用时；**留空显示「其他时间」**。在题型下方填写分项用时后，会自动合计到总时间；手动填写后不再被自动覆盖，清空即恢复自动合计。
- **版本信息面板**：首页新增「版本信息」卡片，显示当前版本，可查看完整版本历史，并可导出版本记录 JSON。
- **版本号贯穿全应用**：页面标题、首页版本标签、版本卡片、备份文件名与备份内容均带上当前版本号；恢复备份时会显示该备份的版本号。
- 新增《版本更新规范.md》：规定每次更新必须遵守的版本号规则、同步点、提交格式与自检清单。
- 新增 `check-version.mjs`：版本一致性校验脚本，一条命令检查代码 / CHANGELOG / README 三处版本号是否一致。
- 新增 `release.ps1`：一键发布脚本，串联「版本校验 → 暂存复核 → 提交 → 推送 GitHub → 验证」；版本校验不通过或暂存区含禁止提交文件时直接中止，不会产生提交与推送。
- 新增项目文档 `README.md`（功能特性、快速开始、目录结构、使用说明、数据存储、常见问题）与 `代码审查报告.md`（全量代码审查，19 项分级发现）。
- 建立本版本修改记录文件 `CHANGELOG.md`。

### 变更
- **确立推送要求**：每次更新完成后，新版本**必须**推送至 GitHub 仓库 <https://github.com/IUNLX/kaoyan-multi-brush>（`origin` / `master`）；只提交不推送视为本次更新未完成。已写入《版本更新规范.md》核心原则与第六节。
- 版本号改为**单一数据源**：仅在脚本顶部 `APP_VERSION` 定义一次，界面各处由 `renderAppVersion()` 统一渲染，消除多处硬编码。
- 版本号规范统一为完整三段式语义化版本（`1.1.0`），与 CHANGELOG / README 保持一致。
- 备份文件名格式改为 `考研数据备份_v<版本号>_<日期>.json`。
- `.gitignore` 由 UTF-16 重写为 UTF-8，并新增 `*.lnk` 忽略规则。

### 修复
- 修复总时间「显示值」与「导出值」不一致的问题：原实现下总时间框显示手动填写值，导出 CSV 却优先取分项合计，二者可能不同。
- 修复填写分项用时后总时间框不联动更新的问题（旧代码对 `<input>` 元素设置 `textContent`，对输入框无效）。
- 修复 `.gitignore` 因编码为 UTF-16 而**全部忽略规则失效**的问题：此前 `*.xlsx`、`*.bak`、`Thumbs.db` 等规则实际均未生效（`git check-ignore` 不匹配），现已改用 UTF-8 并验证通过。
- 修复版本历史中日期错误（原记为 `2025-06-23`，与项目实际发版日期不符）。

---

## [1.0.0] - 2026-09-22

首个稳定版本，作为后续改动的对照基线。当前已具备的能力：

### 新增
- **多科目首页**：英语一 / 数学一 / 408 / 政治四科卡片入口，含各科题量与进度概览。
- **多刷记录表**：按试卷 × 轮次记录得分、分项得分、用时、日期与备注；支持「第 N 刷」轮次的增删。
- **统计面板**：完成数、平均分、最高/最低分、环比涨跌等指标，随录入实时刷新。
- **图表分析**：基于 ECharts 绘制分数与用时的多维趋势图，支持切换统计维度。
- **试卷管理**：试卷的新增、重命名、删除、拖动排序，以及「参与统计」开关（不参与的试卷不计入统计与图表）。
- **数据导入导出**：记录与计划均支持 CSV 导入导出、全量备份与恢复。
- **学习计划模块**：分阶段（基础/强化/冲刺等）录入计划与总结，日期区间自动对齐，含考前倒计时与「今日计划」提醒。
- **本地存储**：数据保存在浏览器 `localStorage`，键名 `kaoyan_<科目>_v1`、`kaoyan_target_<科目>`、`kaoyan_plan_v1`。
- **旧数据兼容**：内置旧格式记录的自动迁移逻辑。

### 说明
- 项目为单文件应用 `考研真题多刷记录.html`，图表依赖 CDN 加载的 ECharts，另有本地 `echarts.min.js` 备用。
- 数据存储在浏览器本地，清除浏览器数据前请先执行「全量备份」。

---

## 条目模板（复制后填写）

```markdown
## [x.y.z] - YYYY-MM-DD

### 新增
- 描述新增的功能或文件。

### 变更
- 描述对既有行为的调整。

### 修复
- 描述修复的问题，必要时附上复现条件。

### 移除
- 描述被删除的功能或文件，并说明替代方案。
```

<!-- 版本链接区：如日后启用远程仓库可在此补充 -->
<!-- [未发布]: https://example.com/compare/v1.0.0...HEAD -->
<!-- [1.0.0]: https://example.com/releases/tag/v1.0.0 -->
